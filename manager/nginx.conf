events {}

http {
  server {
    listen 443 ssl;

    ssl_certificate     /etc/nginx/certs/portainer.crt;
    ssl_certificate_key /etc/nginx/certs/portainer.key;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Timeouts para segurar o túnel
    proxy_read_timeout 3600s;
    proxy_send_timeout 3600s;

    # WebSocket DO AGENT na raiz
    location / {
      proxy_pass http://portainer:8000/;
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }

    # UI do Portainer acessível por /portainer/
    location /portainer/ {
      proxy_pass https://portainer:9443/;
      proxy_ssl_verify off;
      proxy_http_version 1.1;
      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      proxy_set_header X-Forwarded-Proto $scheme;
    }
  }
}
